# 参与开发

## 本地流程

1. 阅读 `决策文档.md`、`CONTEXT.md` 以及相关 ADR。
2. 执行 `npm install` 和 `npm run check`。
3. 在微信开发者工具中导入仓库根目录并完成预览。
4. UI 变更同时检查浏览器验收预览；AR 变更必须在微信开发者工具和代表性 Android、iOS 真机上验证。

## 变更原则

- 保持商品数据、追踪逻辑与渲染实现分离。
- 按佩戴品类组织追踪配置，避免在页面内硬编码锚点算法。
- 不将交易、支付、订单或站外跳转引入 MVP。
- 记录设备、光照、帧率和正反判定限制。

提交主题使用简短祈使句，例如 `feat(ar): anchor bracelet to wrist`。
