# Domain Context

本仓库使用单一上下文，描述“AR 首饰试戴展示”领域。产品决策的最终来源是 `决策文档.md`。

## 核心词汇

- **商家（Merchant）**：上传商品资料和 3D 模型、向平台购买服务的珠宝经营者。
- **用户（Visitor）**：从广告、直播或视频号进入小程序并体验商品的人。不要称为“买家”，因为平台不处理交易。
- **商品（Product）**：属于一个商家的可展示首饰，包含品类、材质、克重、真实尺寸、图片与模型引用。
- **佩戴品类（Wearable Category）**：项链、耳环、手镯、手链或脚链。戒指不属于首期范围。
- **试戴会话（Try-on Session）**：用户打开摄像头、选择商品并让模型跟随目标部位的一次连续体验。
- **追踪锚点（Tracking Anchor）**：VisionKit 关键点转换后供渲染层使用的位置与朝向。
- **正反面（Facing Surface）**：根据目标部位朝向选择模型的正面或背面，并通过迟滞阈值避免边界抖动。
- **模型资产（Model Asset）**：商家上传或平台代建的 GLB/glTF 文件及其材质贴图。
- **尺寸校准（Scale Calibration）**：以商家填写的真实毫米尺寸和模型包围盒计算渲染缩放。

## 边界

- 小程序负责商品展示、商家资料录入和 AR 试戴体验。
- 微信云开发负责早期业务数据与小型云函数；腾讯云 COS/CDN 负责 3D 模型分发。
- 支付、订单、售后、站外跳转、搜索推荐和戒指选指均不在当前上下文内。

## 关键约束

- AR 层只接收经过归一化的锚点，不直接读取商品数据。
- 渲染层不拥有追踪会话生命周期。
- 商品真实尺寸是必填业务数据；生成式模型的尺寸不能作为事实来源。
- 未接入真实 VK 跟踪或 three.js 渲染时，界面必须明确显示“技术接入中”，不能伪装为已经运行。
